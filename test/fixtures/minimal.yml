openapi: "3.0.3"
info:
  title: Minimal API
  version: "1.0"
paths:
  /health:
    get:
      operationId: getHealth
      responses:
        "200":
          description: Health check
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                required:
                  - status
components:
  schemas: {}
